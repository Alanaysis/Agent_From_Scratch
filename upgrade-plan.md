晶圆检测 Recipe Agent 架构升级借鉴清单
（面向编码 Agent 的可执行参考，完全对齐你现有技术栈：Node.js/TS + Electron + React + YAML 工作流 + gRPC 工业软件对接）
总览速查表
表格
架构模块	对应你现有能力	首选借鉴项目	核心借鉴方向	次选参考
Agent 核心执行引擎	YAML 工作流解析与任务调度	Cline (@cline/sdk)	Plan/Act 双模式、状态机、持久化、插件化内核	SWE-agent（仅流程设计思想）
工具与系统对接层	gRPC 工具封装、原有软件交互	Cline Tool 规范 + OpenCode MCP 思想	标准化 Tool 接口、参数校验、审计埋点、可扩展	-
前端可视化交互	React 桌面 UI	OpenHands 前端体系	三栏布局、步骤时间线、人机审批、审计面板	Cline Webview（通信机制）
架构工程化	Electron 主 / 渲染进程架构	OpenCode 分层架构 + Cline 宿主隔离	核心与宿主解耦、类型安全通信、配置化工作流	-
分模块详细借鉴说明
一、Agent 核心执行引擎层（P0 优先级，架构升级核心）
对应现有模块：YAML 工作流调度、任务执行主逻辑
首选借鉴对象：Cline (@cline/sdk)
Plan / Act 双阶段执行模式
借鉴内容：将「任务规划」和「步骤执行」拆分为独立阶段，Plan 阶段输出结构化执行计划，Act 阶段按计划分步执行
落地映射：Plan 阶段加载内置 YAML 工作流，生成 Recipe 生成的标准步骤清单；Act 阶段逐步骤调用 gRPC 工具执行，关键工艺节点自动暂停等待人工确认
状态机驱动的执行循环
借鉴内容：用有限状态机管理任务生命周期（待执行→进行中→待审批→成功 / 失败），统一处理重试、回退、中断逻辑
落地映射：替代现有简单顺序执行，支持单步重跑、步骤跳过、异常回滚，适配长周期的仿真验证任务
会话持久化与断点续跑
借鉴内容：基于 SQLite 做全量会话快照，每执行完一步持久化状态，支持中断后从断点恢复
落地映射：解决晶圆 Recipe 生成耗时长、中途需人工修改参数的场景，支持关闭软件后下次继续执行
插件化内核设计
借鉴内容：核心执行引擎只保留通用调度逻辑，业务规则通过插件注入
落地映射：将晶圆检测领域规则、YAML 模板解析、权限校验等业务逻辑做成独立插件，与 Agent 核心解耦，后续新增工艺不用修改核心代码
二、工具与系统对接层（P0 优先级，稳定性核心）
对应现有模块：gRPC 工具封装、原有工业软件对接
首选借鉴对象：Cline Tool 接口规范 + OpenCode MCP 架构思想
标准化 Tool 抽象接口
借鉴内容：每个工具统一包含「输入 JSON Schema 定义、参数自动校验、执行函数、错误处理、结果格式化」五层结构
落地映射：将现有每个 gRPC 接口按此规范封装，自动校验参数合法性（参数范围、格式等），非法请求直接拦截，不发给下游工业软件，保障工艺安全
工具调用全链路审计
借鉴内容：每次工具调用自动记录入参、出参、耗时、调用方、错误信息
落地映射：满足半导体行业可追溯要求，所有 gRPC 调用自动留痕，可在前端审计面板回溯
统一错误处理与重试策略
借鉴内容：按错误类型分级处理（参数错误直接终止、网络错误自动重试、业务错误暂停等待人工处理）
落地映射：替代现有零散的错误处理逻辑，统一 gRPC 调用的超时、重试、降级策略
MCP 兼容的扩展设计
借鉴内容：工具层与 Agent 核心通过标准协议交互，不硬编码到执行逻辑中
落地映射：当前仅对接现有检测软件，预留标准接口，未来对接 MES、数据库、其他设备时无需改造核心引擎
三、前端可视化交互层（P1 优先级，体验升级核心）
对应现有模块：React 渲染进程 UI
首选借鉴对象：OpenHands 前端交互体系
三栏式经典布局
借鉴内容：左侧「执行步骤时间线」、中间「主内容区（参数 / 预览 / 结果）」、右侧「日志与审计面板」
落地映射：替代现有单页式布局，左侧树形展示 Recipe 生成全流程节点状态，中间展示当前步骤的晶圆图像、参数配置、仿真结果，右侧展示 gRPC 调用明细和系统日志
步骤级人机协同交互
借鉴内容：支持在任意步骤暂停、人工修改参数、单步重跑、跳过当前步骤、终止任务
落地映射：在 YAML 标记的关键工艺节点自动暂停，工程师确认参数后再继续，兼顾自动化和工艺安全性
全链路审计面板
借鉴内容：可展开查看每一步的完整调用详情，支持原始请求 / 响应查看、错误堆栈定位
落地映射：对接工具层的审计日志，方便调试和工艺追溯
任务历史与会话管理
借鉴内容：历史任务列表、多维度筛选、任务回放、结果导出
落地映射：支持按晶圆型号、批次、操作人员检索历史 Recipe 生成记录，可回放执行过程，导出 Recipe 文件和审计报告
四、架构工程化与可扩展性（P2 优先级，长期迭代）
对应现有模块：Electron 主进程 / 渲染进程架构
首选借鉴对象：OpenCode 分层架构 + Cline 宿主隔离设计
核心引擎与宿主环境解耦
借鉴内容：Agent Runtime 作为独立的纯 TS 模块，不依赖 Electron API，可独立运行
落地映射：当前运行在 Electron 主进程，未来需要 Web 端、服务端部署时，核心引擎代码可直接复用，仅需修改宿主层
类型安全的前后端通信
借鉴内容：主进程与渲染进程之间通过预定义消息协议通信，全链路 TypeScript 类型约束
落地映射：规范现有 Electron 的 ipcMain/ipcRenderer 通信，统一事件格式、状态同步机制，避免零散的消息传递
配置化工作流管理
借鉴内容：工作流定义与执行引擎分离，通过配置文件（YAML）定义流程，引擎通用执行
落地映射：强化现有 YAML 工作流的表达能力，支持条件分支、循环、异常跳转，无需修改代码即可新增 Recipe 生成流程
避坑指南：不需要借鉴的部分
聚焦工业领域 Agent 特性，避免引入通用 Coding Agent 的冗余能力：
通用代码编辑能力：无需借鉴文件读写、代码 diff、Git 集成等能力，你的 Agent 核心是生成 Recipe，不是编写代码
TUI 终端交互：OpenCode/Aider 的终端 UI 无需参考，你是桌面可视化产品
多模型自由切换：企业内部通常固定模型，无需做 70 + 模型适配，保留 1-2 个模型对接即可
Claude Code 泄露源码：仅可参考架构思想，严禁直接复用代码，规避版权风险
升级优先级建议
P0（必须先做）：工具层标准化改造 + 执行引擎引入 Plan/Act 模式和状态机
P1（体验升级）：前端三栏布局重构 + 步骤级人机审批交互
P2（长期迭代）：核心引擎与宿主解耦 + 插件化改造 + 会话持久化